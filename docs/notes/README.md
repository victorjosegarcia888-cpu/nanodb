# Notas del proyecto

Este directorio está reservado para decisiones de modelado, resultados de validación y notas que no sean fuentes bibliográficas ni tablas de parámetros originales.

Cada nota nueva debería indicar fecha, hipótesis, unidades, fuente y estado (`borrador`, `validado` o `descartado`).