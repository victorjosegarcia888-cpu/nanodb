# OpenVDB and NanoVDB bridge

Use the existing `nanodb/nanovdb` and `openvdb` trees here through adapters;
do not copy or fork their upstream implementations. The bridge should support
host-side validation first and CUDA device transfer second.